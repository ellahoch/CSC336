function cmtoin() {
    let conversion = 2.54;
    let length = 39;
    let inlength = length * conversion;
    document.write("Length in inches: " + length + "\n");
    document.write("\n length in cm: " + inlength);
}